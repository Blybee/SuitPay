{
  "_comment": "Coding Agent Context Extension Configuration. Escrito como JSON, que es un subconjunto valido de YAML, para que el script de PowerShell pueda leerlo con ConvertFrom-Json sin necesidad de Python con PyYAML ni del modulo powershell-yaml. Si algun dia se instala cualquiera de los dos, este archivo puede volver a escribirse como YAML con comentarios.",

  "_comment_context_file": "QUE: el archivo de contexto del agente, relativo a la raiz del proyecto (la carpeta que contiene .specify/). Se rechazan rutas absolutas, separadores con barra invertida y segmentos '..'. REQUISITO: opcional; si se deja vacio se usa el valor por defecto de la integracion elegida al configurar Spec Kit, definido en agent-context-defaults.json. Aqui se fija de forma explicita el valor que corresponde a la integracion cursor-agent, para que el comportamiento no dependa de la deteccion automatica.",
  "context_file": ".cursor/rules/specify-rules.mdc",

  "_comment_context_files": "QUE: lista de archivos de contexto a mantener sincronizados, relativos a la raiz del proyecto. Si esta rellena, tiene prioridad sobre context_file. REQUISITO: opcional; util solo cuando el proyecto necesita varios archivos de contexto.",
  "context_files": [],

  "_comment_context_markers": "QUE: delimitadores de la seccion gestionada por Spec Kit. La extension solo escribe entre estas marcas y no toca el resto del archivo. REQUISITO: opcional; cambiar solo si se quieren marcas propias.",
  "context_markers": {
    "start": "<!-- SPECKIT START -->",
    "end": "<!-- SPECKIT END -->"
  }
}
