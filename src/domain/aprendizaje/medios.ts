/** Techo por archivo que viaja al modelo (no se persiste). */
export const TECHO_MEDIO_ENTRENAMIENTO_BYTES = 8 * 1024 * 1024

/** Suma de todos los archivos de un lado (pedido u oro). */
export const TECHO_MEDIOS_ENTRENAMIENTO_BYTES = 24 * 1024 * 1024

/** Fotos o PDFs por lado. El pedido del cliente suele llegar en varias imágenes. */
export const MAX_MEDIOS_ENTRENAMIENTO = 8
